$(window).bind("beforeunload",function(event) {
    return "Do Not Leave or Refresh this Pages,following The Time , Upload Payment Screenshot then Click Submit Button ,when you submiting if show notified message just ignore";
});
